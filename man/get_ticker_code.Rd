% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvc-legacy-data.R
\name{get_ticker_code}
\alias{get_ticker_code}
\title{Extraire le code interne d'un ticker depuis la Bourse de Casablanca}
\usage{
get_ticker_code(ticker)
}
\arguments{
\item{ticker}{Un caractère (\code{character}). Le symbole du ticker tel qu'utilisé
sur le site de la Bourse de Casablanca.}
}
\value{
Un vecteur de caractères contenant le(s) code(s) numérique(s) extrait(s)
depuis les liens HTML. Retourne \code{NULL} si la requête échoue ou si la page
n'est pas accessible.
}
\description{
Cette fonction permet de récupérer le code numérique interne (instrument ID)
associé à un ticker donné en interrogeant la page web correspondante sur
le site de la Bourse de Casablanca.
}
\details{
Elle effectue une requête HTTP vers la page de l'instrument, parse le HTML,
puis extrait le code présent dans les attributs \code{href} des éléments dont
l'identifiant commence par "link-".

La fonction utilise les packages \code{httr} pour effectuer la requête HTTP
et \code{rvest} pour parser le contenu HTML.

En cas d'échec de la requête (erreur ou code HTTP différent de 200),
un message est affiché et la fonction retourne \code{NULL}.
}
\examples{
\dontrun{
get_ticker_code("AFM")
}

}
