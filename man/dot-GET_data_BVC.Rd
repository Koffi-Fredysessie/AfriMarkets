% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bvc-legacy-data.R, R/gse-legacy.R
\name{.GET_data_BVC}
\alias{.GET_data_BVC}
\title{Récupération des données historiques des instruments de la Bourse de Casablanca}
\usage{
.GET_data_BVC(
  ticker = "ALL",
  Period = "daily",
  from = Sys.Date() - 89,
  to = Sys.Date(),
  output_format = c("by_col", "by_row")
)

.GET_data_BVC(
  ticker = "ALL",
  Period = "daily",
  from = Sys.Date() - 89,
  to = Sys.Date(),
  output_format = c("by_col", "by_row")
)
}
\arguments{
\item{ticker}{Character vector of stock tickers. Use \code{"ALL"} to retrieve all available tickers.}

\item{Period}{Character string defining data frequency (currently not used, default is "daily").}

\item{from}{Start date for data extraction (Date or character).}

\item{to}{End date for data extraction (Date or character).}

\item{output_format}{Output structure: \code{"by_col"} (default) for long format,
or \code{"by_row"} for wide format.}
}
\value{
Un dataframe contenant les données historiques des instruments :
\itemize{
\item Date de transaction (\code{created})
\item Prix (ouverture, clôture, haut, bas)
\item Volume et nombre de transactions
\item Capitalisation et autres indicateurs
}

A data frame containing historical stock market data with standardized columns:
Date, Ticker, YearHigh, YearLow, PrevCloseVWAP, OpenPrice, LastPrice,
CloseVWAP, PriceChange, BidClose, AskClose, Volume, ValueTraded.
}
\description{
Cette fonction permet d'extraire les données historiques de marché (prix, volume,
capitalisation, etc.) pour un ou plusieurs tickers depuis l'API de la Bourse de Casablanca.

This function extracts historical daily stock market data from the Ghana Stock Exchange (GSE)
web portal for one or multiple tickers. It sends a POST request to the GSE DataTables API,
retrieves the raw JSON response, and formats it into a clean data frame.
}
\details{
Elle prend en charge l'extraction pour un ticker spécifique ou l'ensemble des tickers disponibles,
sur une période donnée, et retourne les données sous forme de dataframe.

La fonction :
\enumerate{
\item Récupère la liste des tickers disponibles via \code{GET_tickers()}
\item Convertit les tickers en codes internes via \code{get_ticker_code()}
\item Interroge l'API REST de la Bourse de Casablanca
\item Agrège les données pour chaque ticker
\item Nettoie les valeurs manquantes et convertit les types
}

Un délai aléatoire (\code{Sys.sleep}) est introduit entre les requêtes pour éviter
les blocages côté serveur.

The function performs the following steps:
\enumerate{
\item Retrieves the list of available tickers using \code{GET_tickers("GSE")}.
\item Filters input tickers or uses all available tickers when \code{"ALL"} is specified.
\item Sends an HTTP POST request to the GSE web portal DataTables endpoint.
\item Extracts and parses JSON response data.
\item Renames and standardizes column names.
\item Optionally reshapes output either by column or by row.
}
}
\note{
This function relies on a private web endpoint of the GSE portal and may break if
the underlying HTML structure or API changes. It includes retry handling and basic
error messages for unavailable tickers.
}
\examples{
\dontrun{
df <- .GET_data_BVC(ticker = "IAM")
df <- .GET_data_BVC(ticker = c("IAM", "BCP"))
df <- .GET_data_BVC(ticker = "ALL")
df <- .GET_data_BVC(ticker = c("IAM", "BCP"), output_format = "by_row")
}

}
\keyword{internal}
